---
interface Props {
  size?: number;
  class?: string;
}
const { size = 48, class: className = "" } = Astro.props;
---

<img
  src="/images/logo.png"
  alt="JCBO Conseil"
  width={size}
  height={Math.round(size * 1.17)}
  class={`object-contain flex-shrink-0 ${className}`}
/>

